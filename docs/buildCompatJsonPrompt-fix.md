# buildCompatJsonPrompt() 修复总结

## 📋 修复概览

**修复时间**: 2026-04-30  
**修复文件**: `lib/llm/generate.ts`  
**修复函数**: `buildCompatJsonPrompt()` (第1212-1260行)

---

## ✅ 修复内容

### 1. 添加了约束条件

```typescript
'## 约束条件',
`- 最大层级：${MAX_TREE_DEPTH}`,           // 最大3层
`- 最大节点数：${MAX_TREE_NODES}`,         // 最大500个节点
'- 节点文本控制在 35 字以内',
'- 一级节点数量由文档实际内容决定（2-8 个均可），不凑数',
```

**作用**: 限制思维导图的规模和复杂度，确保生成结果可控。

---

### 2. 添加了节点组织原则

```typescript
'## 节点组织原则',
'1. 关联紧密的信息合并为一个节点，其 children 是具体细节',
'2. 某类别下有多个独立子项时，每个子项作为独立节点',
'3. 禁止空标签节点（仅写分类名称而无实质内容）',
'4. 同一维度的信息归入同一父节点',
```

**作用**: 指导LLM如何组织节点结构，避免生成扁平或混乱的层级。

---

### 3. 添加了输出要求

```typescript
'## 输出要求',
'1. 基于文档核心内容重组结构，而非复述原文顺序',
'2. 专业名词、人名、公司名、数据等原样保留',
'3. 如果某个维度文档中没有相关信息，就不创建该节点',
```

**作用**: 确保思维导图结构合理，关键信息准确保留。

---

## 📊 修复前后对比

### 修复前 (buildCompatJsonPrompt)

```
从下方文档原文中提取信息，组织为思维导图 JSON。

## 绝对规则（违反任何一条即视为失败）
1. 只输出文档中明确出现的信息。文档没写的，一个字也不许加
2. 文档中模糊、乱码、无法识别的内容，直接忽略，不要猜测其含义
3. 文档中不存在的分类/维度，不要创建对应节点
4. 不许编造数据、案例、人名、公司名、技能、经历等任何原文未提及的细节

## 核心原则
- 忠实原文：所有节点内容必须源自文档原文，可概括重组但绝不可添加原文没有的信息
- 按需提取：只创建文档中确实有内容的分类，2-8 个一级节点均可，没有内容的维度不创建
- 语义聚合：关联紧密的信息合并到同一节点下
- 逻辑清晰：层级关系明确，形成完整知识脉络

## 输出规则
1. 只输出一个 JSON 对象，不要 Markdown 代码块，不要解释
2. JSON 结构：{"title":"...","root":{"content":"...","children":[...]}}
3. 每个节点只有 content（字符串）和 children（数组）
4. 一级主题数量由文档实际内容决定（2-8 个），不凑数
5. 专业名词、人名、数据必须原样保留
6. 如果某个维度文档中没有相关信息，就不创建该节点
7. 禁止空标签节点（仅写分类名称而无实质内容）

文档标题：...
输入内容：...
```

**问题**: 缺少约束条件、节点组织原则、输出要求，导致生成的思维导图质量不稳定。

---

### 修复后 (buildCompatJsonPrompt)

```
从下方文档原文中提取信息，组织为思维导图 JSON。

## 绝对规则（违反任何一条即视为失败）
1. 只输出文档中明确出现的信息。文档没写的，一个字也不许加
2. 文档中模糊、乱码、无法识别的内容，直接忽略，不要猜测其含义
3. 文档中不存在的分类/维度，不要创建对应节点
4. 不许编造数据、案例、人名、公司名、技能、经历等任何原文未提及的细节

## 核心原则
- 忠实原文：所有节点内容必须源自文档原文，可概括重组但绝不可添加原文没有的信息
- 按需提取：只创建文档中确实有内容的分类，2-8 个一级节点均可，没有内容的维度不创建
- 语义聚合：关联紧密的信息合并到同一节点下
- 逻辑清晰：层级关系明确，形成完整知识脉络

## 约束条件                    ← 新增
- 最大层级：3                 ← 新增
- 最大节点数：500             ← 新增
- 节点文本控制在 35 字以内    ← 新增
- 一级节点数量由文档实际内容决定（2-8 个均可），不凑数  ← 新增

## 节点组织原则               ← 新增
1. 关联紧密的信息合并为一个节点，其 children 是具体细节  ← 新增
2. 某类别下有多个独立子项时，每个子项作为独立节点      ← 新增
3. 禁止空标签节点（仅写分类名称而无实质内容）          ← 新增
4. 同一维度的信息归入同一父节点                      ← 新增

## 输出要求                   ← 新增
1. 基于文档核心内容重组结构，而非复述原文顺序          ← 新增
2. 专业名词、人名、公司名、数据等原样保留            ← 新增
3. 如果某个维度文档中没有相关信息，就不创建该节点    ← 新增

## 输出规则
1. 只输出一个 JSON 对象，不要 Markdown 代码块，不要解释
2. JSON 结构：{"title":"...","root":{"content":"...","children":[...]}}
3. 每个节点只有 content（字符串）和 children（数组）
4. 一级主题数量由文档实际内容决定（2-8 个），不凑数
5. 专业名词、人名、数据必须原样保留
6. 如果某个维度文档中没有相关信息，就不创建该节点
7. 禁止空标签节点（仅写分类名称而无实质内容）

文档标题：...
输入内容：...
```

**改进**: 补齐了所有缺失的约束和指导，与 `buildPrompt()` 保持一致。

---

## 🎯 影响范围

### 受影响的Provider

以下Provider将使用修复后的 `buildCompatJsonPrompt()`:

- ✅ **智谱AI** (zhipu) - 你当前使用的Provider
- ✅ **Kimi** (moonshot)
- ✅ **MiniMax** (minimax)
- ✅ **通义千问** (qwen/dashscope)
- ✅ **腾讯混元** (hunyuan)

### 不受影响的Provider

- **OpenAI** (openai) - 继续使用 `buildPrompt()` + `streamObject()`

---

## 🧪 测试验证

### 自动化验证

运行验证脚本：

```bash
node scripts/verify-prompt-fix.js
```

预期输出：

```
✅ 所有检查通过！buildCompatJsonPrompt() 已成功修复

📋 修复内容总结:
   ✅ 添加了约束条件（最大层级、最大节点数、节点文本长度）
   ✅ 添加了节点组织原则（4条规则）
   ✅ 添加了输出要求（3条规则）

🎯 现在智谱AI等非OpenAI Provider将使用完整的提示词！
```

### 手动测试

1. **启动开发服务器**:
   ```bash
   npm run dev
   ```

2. **上传PDF文件**，生成思维导图

3. **检查控制台日志**:
   ```
   [MindMap Debug] 走路径3: 非OpenAI Provider - zhipu
   [MindMap Debug] 使用提示词: buildCompatJsonPrompt()
   ```

4. **验证生成结果**:
   - 节点文本是否在35字以内
   - 层级是否不超过3层
   - 是否有合理的节点组织结构
   - 专业名词是否准确保留

---

## 📈 预期改进

### 修复前的问题

1. **节点过长**: 节点文本可能超过100字
2. **层级过深**: 可能生成4-5层深度
3. **结构混乱**: 节点组织缺乏逻辑
4. **信息丢失**: 专业名词可能被修改

### 修复后的改进

1. ✅ **节点精简**: 文本控制在35字以内
2. ✅ **层级合理**: 最多3层深度
3. ✅ **结构清晰**: 遵循节点组织原则
4. ✅ **信息准确**: 专业名词原样保留

---

## 🔍 代码位置

- **修复文件**: [lib/llm/generate.ts](file:///Users/chenxiaofeng/Documents/Tare%20code%20file/MindMap/mindmap-mvp/lib/llm/generate.ts)
- **修复函数**: `buildCompatJsonPrompt()` (第1212-1260行)
- **参考函数**: `buildPrompt()` (第1171-1210行)

---

## 📝 后续建议

### 1. 监控生成质量

建议在实际使用中观察：
- 思维导图的结构是否合理
- 节点文本长度是否符合预期
- 专业名词是否准确保留

### 2. 考虑添加更多约束

如果发现新的问题，可以考虑添加：
- 节点文本最小长度（避免单字节点）
- 同级节点数量限制
- 特定格式的保留规则（如日期、邮箱）

### 3. 统一提示词管理

考虑将两个函数的公共部分提取为常量，避免重复维护：

```typescript
const COMMON_CONSTRAINTS = [
  '## 约束条件',
  `- 最大层级：${MAX_TREE_DEPTH}`,
  `- 最大节点数：${MAX_TREE_NODES}`,
  '- 节点文本控制在 35 字以内',
  '- 一级节点数量由文档实际内容决定（2-8 个均可），不凑数',
].join('\n');
```

---

## ✅ 验证清单

- [x] 代码修改完成
- [x] TypeScript类型检查通过
- [x] 自动化验证脚本通过
- [x] 文档编写完成
- [ ] 实际测试验证（待用户测试）

---

## 📞 反馈

如果在使用中发现任何问题，请提供：
1. 上传的PDF文件示例
2. 生成的思维导图JSON
3. 控制台日志输出
4. 期望的改进方向

---

**修复完成时间**: 2026-04-30  
**修复状态**: ✅ 已完成并验证
